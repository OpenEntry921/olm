# 공통 자산

브랜드 공통 아이콘은 `icons/`, 배경은 `backgrounds/`, 1200×630 공유 이미지는 `og/`에 보관합니다. SVG를 아이콘에 우선 사용하고 사진형 배경·OG 이미지는 WebP를 우선 사용합니다. 파일명은 영문 소문자와 하이픈으로 `brand-category-feature-size-view.ext` 형식을 따르며 원본 편집 파일은 보관하지 않습니다.
